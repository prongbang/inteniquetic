# Advanced workflow feature
# https://docs.github.com/en/actions/using-workflows/advanced-workflow-features

name: WEB CI/CD

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ master ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@f2a1d5e99d037542a71f64918e516c093c6f3fc4
        with:
          context: deployments/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

# on self-hosted
#jobs:
#
#  setup:
#    runs-on: self-hosted
#    steps:
#      - run: echo "Setup"
#
#  build:
#    needs: setup
#    runs-on: self-hosted
#    steps:
#      - uses: actions/checkout@v2
#      - name: Build the Docker image
#        run: docker build . --file deployments/Dockerfile --tag prongbang/wachasit:1.0
#
#  deploy:
#    needs: build
#    runs-on: self-hosted
#    steps:
#      - name: Deploy the Docker image
#        run: |
#          # env wachasit_tag=$(date +%s)
#          docker stack deploy -c deployments/web-prod.yml --resolve-image always wachasit
#          docker service update wachasit_wachasit --image prongbang/wachasit:1.0 --force
#
#  clean:
#    needs: deploy
#    runs-on: self-hosted
#    steps:
#      - name: Clean the Docker image
#        run: docker system prune -f
